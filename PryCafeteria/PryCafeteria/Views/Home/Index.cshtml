@{
    ViewData["Title"] = "Inicio";
}

<div class="text-center py-5">
    <img src="~/images/productos/Bebidas_Calientes/Cafe_Latte.png"
         alt="Starbucks"
         style="width:120px; height:120px; object-fit:cover; border-radius:50%; margin-bottom:1.5rem;"
         onerror="this.style.display='none'" />

    <h1 class="display-5 fw-bold text-cafe mb-2">Bienvenido a Starbucks</h1>
    <p class="text-muted mb-4">Tu café favorito, siempre listo para ti.</p>

    @if (User.Identity?.IsAuthenticated == true)
    {
        <p class="fs-5 mb-4">
            Hola, <strong>@(User.FindFirst("Nombre")?.Value ?? User.Identity?.Name)</strong> 👋
        </p>
        <a asp-controller="Productos" asp-action="Index" class="btn btn-cafe btn-lg px-5">
            Ver Productos
        </a>
    }
    else
    {
        <div class="d-flex justify-content-center gap-3">
            <a asp-controller="Cuentas" asp-action="Login" class="btn btn-cafe btn-lg px-5">
                Iniciar Sesión
            </a>
            <a asp-controller="Cuentas" asp-action="Registro" class="btn btn-outline-secondary btn-lg px-5">
                Registrarse
            </a>
        </div>
    }
</div>
